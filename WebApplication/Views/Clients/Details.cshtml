@model WebApplication.Models.Client

@{
    ViewData["Title"] = "Details";
}

<h2>
    Detalhes
</h2>

<div>
    <h4>
        Cliente
    </h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Name)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-primary glyphicon glyphicon-pencil" title="Editar"></a>
    <a asp-action="Index" class="btn btn-default glyphicon glyphicon-arrow-left" title="Voltar"></a>
</div>
